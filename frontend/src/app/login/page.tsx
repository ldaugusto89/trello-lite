'use client'
import AuthPanel from "@/components/ui/loginRegisterPanel/AuthPanel"

export default function loginPanel() {
    
    return (
        <main className="flex items-center justify-center min-h-screen bg-[#121212]">
            <AuthPanel/>
        </main>
    )
}